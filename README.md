# Draw a Snowflake ❄️

Веб-приложение для создания и анимации снежинок. Рисуйте уникальные снежинки, сохраняйте их в базу данных и наблюдайте, как они падают на рождественское дерево.

## 🎨 Возможности

### Рисование снежинок
- **Инструменты рисования**: кисть и ластик с настраиваемым размером
- **Выбор цвета**: палитра для создания разноцветных снежинок
- **Масштабирование**: зум для детальной проработки
- **История действий**: отмена и повтор операций
- **Анализ похожести**: автоматическая проверка уникальности снежинки
- **Экспорт**: сохранение рисунков в формате PNG и копирование в буфер обмена

### Анимация на дереве
- **Падающие снежинки**: анимация падения ваших рисунков
- **Реалистичная физика**: случайные траектории, вращение и дрейф
- **Управление скоростью**: регулировка скорости анимации
- **Пауза/возобновление**: контроль анимации

### Хранение данных
- Сохранение снежинок в базе данных PostgreSQL
- Загрузка ранее созданных снежинок
- Обновление и удаление сохранённых снежинок

## 🛠 Технологический стек

### Frontend
- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик и dev-сервер
- **Material-UI (MUI)** - компоненты интерфейса
- **Redux Toolkit** - управление состоянием
- **React Router** - маршрутизация
- **Framer Motion** - анимации
- **i18n** - интернационализация (русский/английский)

### Backend
- **Node.js** - серверная среда
- **Express** - веб-фреймворк
- **TypeScript** - типизация
- **Prisma** - ORM для работы с БД
- **PostgreSQL** - база данных
- **Helmet** - безопасность HTTP заголовков
- **Express Rate Limit** - ограничение запросов
- **Sentry** - мониторинг ошибок

### Тестирование
- **Jest** - unit тесты
- **React Testing Library** - тестирование компонентов
- **Playwright** - E2E тесты

### Инструменты разработки
- **ESLint** - линтер кода
- **Prettier** - форматирование кода
- **Stylelint** - линтер стилей

## 📋 Требования

### Для Docker запуска (рекомендуется)
- **Docker** >= 20.x
- **Docker Compose** >= 2.x

### Для локального запуска
- **Node.js** >= 18.x
- **npm** >= 9.x (или yarn/pnpm)
- **PostgreSQL** >= 14.x
- **Git**

## 🚀 Быстрый старт

### 🐳 Вариант 1: Запуск с Docker (рекомендуется)

Самый простой способ запустить проект - использовать Docker Compose. Это запустит все сервисы (PostgreSQL, Backend, Frontend) автоматически.

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd drawasnowflake
```

#### 2. Запуск проекта

```bash
docker compose up --build
```

Эта команда:
- Соберёт Docker образы для backend и frontend
- Запустит PostgreSQL базу данных
- Применит миграции базы данных
- Запустит backend сервер
- Запустит frontend приложение

#### 3. Доступ к приложению

После успешного запуска:
- **Frontend** будет доступен по адресу: http://localhost
- **Backend API** будет доступен по адресу: http://localhost:3001
- **PostgreSQL** будет доступен на порту: 5432

#### Полезные команды Docker Compose

```bash
# Запуск в фоновом режиме
docker compose up -d

# Остановка контейнеров
docker compose down

# Остановка с удалением volumes (удалит данные БД)
docker compose down -v

# Просмотр логов
docker compose logs -f

# Пересборка образов
docker compose build --no-cache

# Остановка всех сервисов
docker compose stop

# Перезапуск сервисов
docker compose restart
```

---

### 💻 Вариант 2: Локальный запуск

Если вы предпочитаете запускать проект локально без Docker:

#### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd drawasnowflake
```

#### 2. Настройка базы данных

Создайте базу данных PostgreSQL:

```bash
createdb drawasnowflake
# или через psql:
psql -U postgres
CREATE DATABASE drawasnowflake;
```

#### 3. Настройка переменных окружения

Создайте файл `backend/.env`:

```env
DATABASE_URL="postgresql://username:password@localhost:5432/drawasnowflake"
PORT=3001
FRONTEND_URL="http://localhost:5173"
SENTRY_DSN="" # опционально, для мониторинга ошибок
```

Создайте файл `frontend/.env` (опционально):

```env
VITE_API_URL="http://localhost:3001/api"
```

#### 4. Установка зависимостей

Используйте Makefile для автоматической установки:

```bash
make install
```

Или вручную:

```bash
# Установка зависимостей frontend
cd frontend
npm install

# Установка зависимостей backend
cd ../backend
npm install
```

#### 5. Настройка Prisma

Сгенерируйте Prisma клиент и выполните миграции:

```bash
make prisma-setup
```

Или вручную:

```bash
cd backend
npx prisma generate
npx prisma migrate dev
```

#### 6. Запуск приложения

##### Использование Makefile (рекомендуется)

Запуск frontend и backend одновременно:

```bash
make dev
```

Или запуск отдельных частей:

```bash
# Только backend
make backend

# Только frontend
make frontend
```

##### Ручной запуск

Запустите backend (в одном терминале):

```bash
cd backend
npm run dev
```

Запустите frontend (в другом терминале):

```bash
cd frontend
npm run dev
```

После запуска:
- **Frontend** будет доступен по адресу: http://localhost:5173
- **Backend API** будет доступен по адресу: http://localhost:3001

## 📁 Структура проекта

```
drawasnowflake/
├── backend/                 # Backend приложение
│   ├── src/
│   │   ├── controllers/     # Контроллеры
│   │   ├── routes/          # Маршруты API
│   │   ├── middleware/      # Middleware (rate limiting, etc.)
│   │   ├── utils/           # Утилиты (Sentry, etc.)
│   │   └── server.ts        # Точка входа
│   ├── prisma/
│   │   └── schema.prisma    # Схема базы данных
│   └── package.json
│
├── frontend/                # Frontend приложение
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   │   ├── Canvas/      # Компонент canvas для рисования
│   │   │   ├── Tree/        # Компонент дерева с анимацией
│   │   │   └── UI/          # UI компоненты (Toolbar, Header, etc.)
│   │   ├── pages/           # Страницы приложения
│   │   │   ├── DrawPage.tsx # Страница рисования
│   │   │   └── TreePage.tsx # Страница с деревом
│   │   ├── features/        # Redux slices
│   │   │   ├── drawing/     # Состояние рисования
│   │   │   ├── history/     # История действий (undo/redo)
│   │   │   └── snowflake/   # Состояние снежинок
│   │   ├── services/        # API сервисы
│   │   ├── utils/           # Утилиты
│   │   ├── store/           # Redux store
│   │   └── i18n/            # Локализация
│   ├── e2e/                 # E2E тесты (Playwright)
│   └── package.json
│
└── Makefile                 # Команды для разработки
```

## 🧪 Тестирование

### Unit тесты

```bash
# Запуск всех unit тестов
make test

# Или отдельно:
cd frontend && npm test
cd backend && npm test

# С покрытием:
cd frontend && npm run test:coverage
cd backend && npm run test:coverage
```

### E2E тесты

```bash
make test-e2e
# или
cd frontend && npm run test:e2e

# С UI интерфейсом:
cd frontend && npm run test:e2e:ui
```

### Линтинг и форматирование

```bash
# Проверка линтера
make lint

# Автоисправление ошибок линтера
cd frontend && npm run lint:fix
cd backend && npm run lint:fix

# Форматирование кода
make format
```

## 🔧 Доступные команды

### Makefile команды

```bash
make dev          # Запуск frontend и backend в dev режиме
make backend      # Запуск только backend
make frontend     # Запуск только frontend
make install      # Установка всех зависимостей
make prisma-setup # Настройка Prisma (generate + migrate)
make build        # Сборка production версии
make test         # Запуск всех тестов
make test-e2e     # Запуск E2E тестов
make lint         # Проверка линтером
make format       # Форматирование кода
make clean        # Очистка node_modules и dist
```

### Frontend команды

```bash
npm run dev              # Запуск dev сервера
npm run build            # Сборка production
npm run preview          # Превью production сборки
npm test                 # Unit тесты
npm run test:watch       # Тесты в watch режиме
npm run test:coverage    # Тесты с покрытием
npm run test:e2e         # E2E тесты
npm run lint             # Проверка ESLint
npm run lint:fix         # Автоисправление ESLint
npm run format           # Форматирование Prettier
npm run stylelint        # Проверка стилей
npm run stylelint:fix    # Автоисправление стилей
```

### Backend команды

```bash
npm run dev              # Запуск dev сервера с hot reload
npm run build            # Сборка TypeScript
npm start                # Запуск production версии
npm test                 # Unit тесты
npm run test:watch       # Тесты в watch режиме
npm run test:coverage    # Тесты с покрытием
npm run lint             # Проверка ESLint
npm run lint:fix         # Автоисправление ESLint
npm run format           # Форматирование Prettier
npm run prisma:generate  # Генерация Prisma клиента
npm run prisma:migrate   # Выполнение миграций
npm run prisma:studio    # Открытие Prisma Studio (UI для БД)
```

## 🌐 API Endpoints

### Health Check
```
GET /api/health
```

### Snowflakes
```
GET    /api/snowflakes         # Получить все снежинки
GET    /api/snowflakes/:id     # Получить снежинку по ID
POST   /api/snowflakes         # Создать новую снежинку
PUT    /api/snowflakes/:id     # Обновить снежинку
DELETE /api/snowflakes/:id     # Удалить снежинку
```

## 🗄 База данных

### Схема Snowflake

```prisma
model Snowflake {
  id         String   @id @default(cuid())
  x          Float    @default(0)
  y          Float    @default(0)
  rotation   Float    @default(0)
  scale      Float    @default(1)
  pattern    String   @default("custom")
  imageData  String?  // Base64 encoded image data
  fallSpeed  Float?
  driftSpeed Float?
  driftPhase Float?
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
```

### Работа с Prisma

```bash
# Открыть Prisma Studio (GUI для БД)
cd backend && npm run prisma:studio

# Создать новую миграцию
cd backend && npx prisma migrate dev --name migration_name

# Применить миграции в production
cd backend && npx prisma migrate deploy

# Сброс базы данных (осторожно!)
cd backend && npx prisma migrate reset
```

## 🔐 Безопасность

- **Helmet** - защита HTTP заголовков
- **CORS** - настройка cross-origin запросов
- **Rate Limiting** - ограничение количества запросов (100 запросов за 15 минут)
- **Sentry** - мониторинг ошибок в production

## 🌍 Интернационализация

Приложение поддерживает два языка:
- Русский (ru)
- Английский (en)

Язык определяется автоматически на основе настроек браузера.

## 📦 Production сборка

### С использованием Docker (рекомендуется)

Для production запуска используйте Docker Compose:

```bash
# Сборка production образов
docker compose build

# Запуск в production режиме
docker compose up -d

# Просмотр логов
docker compose logs -f
```

Все переменные окружения уже настроены в `docker-compose.yml`. Для production вы можете создать файл `.env` в корне проекта и переопределить переменные:

```env
POSTGRES_PASSWORD=your_secure_password
SENTRY_DSN=your_sentry_dsn
```

### Локальная сборка

#### Frontend

```bash
cd frontend
npm run build
```

Собранные файлы будут в папке `frontend/dist/`. Можно использовать любой статический хостинг (Vercel, Netlify, etc.).

#### Backend

```bash
cd backend
npm run build
npm start
```

Или используйте процесс-менеджер, например PM2:

```bash
npm install -g pm2
cd backend
npm run build
pm2 start dist/server.js
```

## 🤝 Разработка

### Git Workflow

Проект использует Git Flow с ветками:
- `main` - production версия
- `develop` - разработка
- `feature/*` - новые функции
- `fix/*` - исправления багов

### Pull Request

При создании PR используйте шаблон из `.github/pull_request_template.md`.

### CI/CD

Проект использует GitHub Actions:
- `.github/workflows/ci.yml` - проверка кода и тесты
- `.github/workflows/deploy.yml` - деплой

## 📝 Лицензия

[Укажите лицензию проекта]

## 👥 Авторы

[Укажите авторов проекта]

---

**Приятного рисования снежинок! ❄️**
